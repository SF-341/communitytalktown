import Covidapi from './Covidapi'

const DashBoard = () => {

    // if (!currentUser) {
    //     return <Redirect to="/login" />;
    // }

    return (
        <div>
            <Covidapi />
        </div>
    )
}

export default DashBoard;